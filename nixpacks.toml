# nixpacks configuration for Railway deployment
# Build version: 2024-11-24 - Fixed vite base path for root deployment
[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'

